package dev.jka.bommigrate.web;

import org.springframework.boot.autoconfigure.SpringBootApplication;

/**
 * Spring Boot application class for the bom-migrate web UI.
 *
 * <p>This class is not normally used as a standalone {@code main()} entry
 * point. Instead, it is launched programmatically from the CLI via
 * {@link WebServerLauncher#start} when the user passes {@code --web}.
 */
@SpringBootApplication
public class BomMigrateWebApplication {
}
